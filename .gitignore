venv-rag-new/
__pycache__/
*.pyc
.pytest_cache/

# --- Secrets and local config: NEVER commit. .env.example (placeholders only) stays tracked. ---
.env
.env.*
!.env.example
.streamlit/secrets.toml
*.pem
*.key
service-account*.json

# screenshots/ is tracked on purpose (evidence for the README). It holds cropped copies only:
# keep any uncropped originals (address bar, project id, account name) outside the repository.

# --- Local tool state and caches ---
.deepeval/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
*.egg-info/
*.log

# --- Editor and OS files ---
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# --- Claude Code: personal settings stay local ---
.claude/settings.local.json

# --- Personal working notes (not part of the product) ---
improvement_stories.md
context_prompt.md
plannings.md

# --- Local-only demo: the tool-calling pattern behind the admin toggle in chat_app.py ---
src/agentic_demo.py

data/raw/
data/text/
data/prepared/
data/guarded/
data/chunks/
data/embeddings/
# data/images/ is intentionally tracked (not ignored) - the deployed Streamlit UI needs these
# ~174 source figures shipped in the Docker image; Cloud Build clones from GitHub, so anything
# gitignored is invisible to it regardless of what .dockerignore allows.

# MLflow local file store
mlruns/

# Generated run reports (live query / latency / guardrail probes): reproducible, and they contain real answers
reports/
